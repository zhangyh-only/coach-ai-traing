# macOS
.DS_Store

# Large binary course source materials (kept locally only)
课程资料/

# Temporary working files (contact sheets, rendered, scorm, pdfs)
tmp/

# Editor config
.obsidian/

# Large intermediate extraction artifacts (rendered pages/slides, reproducible via extract script)
# 用 **/ 以兼容任意父目录(如 知识库搭建/output_v2/extracted/)
**/output_v2/extracted/

# rig 工作流本地产物（安装报告 / 备份；与 .claude/protected-paths.txt 同源，见 rig manifest）
.rig/
*.bak
**/install-report.json

# 本地联调接口说明（含本地后端地址 / dev 凭证 / 真实测试ID，仅本地保留，勿入库）
# 若日后要入库供团队参考，须先脱敏（凭证/companyCode/真实ID 换占位符）再从本行移除
陪练场景搭建/公共_跨场景复用/本地场景测试接口说明.md
