# 结构 / 组织约定（本项目权威全文）

> 本仓无代码模块 / 包 / 依赖图，只有**目录归档与物料流向**约定。每条标 A/B/C 桶（见 `README.md`）。
> （rig 模板原有的"模块放置 / 包结构 / 依赖矩阵"针对代码分层，本仓 N/A，已替换为下列实际约定。）
> 证据路径一律为仓库根相对全路径，便于按图核对。

## 物料流向  [B 桶 → 注入 + review]
- **上游原料只进 `辅助资料整理/`**（全二进制素材、无 .md）；**工程化 .md 产物只进下游** `知识库搭建/` 与 `陪练场景搭建/`。原料喂下游时**复制而非移动**（如 China Persona 同步复制进场景目录）。 [B]
  - 证据：`辅助资料整理/`（`find -name '*.md'` 为空，全 .xlsx/.docx/.png/.jpg）；commit f297f7f 把素材复制进 `陪练场景搭建/陪练场景要求顾客画像/`。
- **历史 / 上游只读件归只读区**（如 `参考资料/`）：场景搭建_初始输入prompt、百炼空间能力机制分析报告、历史场景.md 等与现行工程产物分开。 [C]
  - 证据：`陪练场景搭建/参考资料/`；`陪练场景搭建/场景搭建_总体设计与进度.md`（输入 / 历史参考定位）。

## 知识库分库与归属  [B 桶 → 注入 + review]
- **物理分库 = KB1~KB6 六库，单容器 + 双标签承载**（不建六个百炼库）：46 篇全传同库，三链用 标签A=KB编号 / 标签B=内容类别 限定召回范围。 [B]
  - 证据：`知识库搭建/Coach陪练知识库架构设计_v2.md`（六库 v2 定稿）；`知识库搭建/百炼知识库部署配置与命中测试清单.md`（建一个库 + 双标签）。
- **KB 文档粒度判据**：「这块知识会不会被某个下游环节独立检索」→ 会则独立成篇。KB 文档是**被检索 / 被提炼的参考材料**，不是被整段复制的内容块。 [B]
  - 证据：`知识库搭建/Coach陪练知识库架构设计_v2.md`（文档粒度约定）。
- **库内归属边界**："服务是什么 / 规则边界"（静态事实）→ KB6 门店服务规范；"销售时如何用服务打动顾客（话术 / 动作）"→ KB3 流程；GenZ 沟通红线与方法**统一在 KB3 单处维护**（不两处漂移）。 [B]
  - 证据：`知识库搭建/Coach陪练知识库架构设计_v2.md`（v2 关键边界规则、D13/D14 GenZ 合并）。

## 场景目录与归档  [B 桶 → 注入 + review]
- **每个场景目录根下提示词只保留正式版一个**（名为 `agent最新使用提示词.md`，**无版本号**）；旧版 / 废稿 / 搁置草案一律移入该场景的 `归档/` 子目录，与线上版隔离。 [B]
  - 证据：`陪练场景搭建/场景1_轻奢包追求者Elena_正价/归档/`（旧版废稿）；`陪练场景搭建/场景1_轻奢包追求者Elena_正价/调优记录.md`（"提示词目录现只剩正式版一个"归档触发条件）。
- **被认定为误判 / 废稿 / 被取代时才归档**，并配套用 git 回退基于废稿产生的衍生内容（如测试报告相关章节），避免废稿污染事实链。 [B]
  - 证据：`陪练场景搭建/场景1_轻奢包追求者Elena_正价/调优记录.md`（2026-06-24：误判稿已归档、对抗测试章节 git 恢复到原始行数）。

## 上线源辨识  [A 桶（篇数）+ B 桶（口径）→ verify-local 提醒]
- **现行上线源 = 顶层 `知识库搭建/具体KB内容/`**（46 篇含 KB6）。**注意 `output_v2/` 不是上线源**：它是上一交付批次（v1.5 / Codex 阶段）的快照与中间稿，其 `交付总览.md` 自报 45 篇 / 五库口径——但**成品 KB 已不在该目录**，那里只剩 `交付总览.md` 与 `course_analysis/`、`extracted/` 等中间产物。 [B]
  - 证据：`知识库搭建/output_v2/` 下仅见 `course_analysis/`、`extracted/` 与 3 个总览 .md，无 `具体KB内容/`；45 篇 / 五库口径仅以文字存在于 `知识库搭建/output_v2/交付总览.md`。实测顶层 `知识库搭建/具体KB内容/` KB1..KB6 = 46 篇。
- **多处版本 / 篇数口径并存，一律以现行为准**：现行架构是 v2.0（六库 46 篇）；`知识库搭建/output_v2/交付总览.md`、`知识库搭建/知识库完善_进展与计划.md` 成文于上一批次（45 篇 / 五库口径）。文档口径认 **v2 架构文档（`知识库搭建/Coach陪练知识库架构设计_v2.md`）+ 变更追溯台账**。 [B]
  - 证据：`知识库搭建/output_v2/交付总览.md` 自报 45 篇五库；现行 `知识库搭建/Coach陪练知识库架构设计_v2.md` 为六库 46 篇定稿。
